services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pfein-api
    env_file:
      - .env
    environment:
      MONGO_URI: mongodb://mongo:27017
      MONGO_DB: ${MONGO_DB:-pfein}
      INGESTION_DATA_DIR: /source-archives
      BODACC_INIT_DATA_DIR: /source-archives/bodacc
      BODACC_CURRENT_DATA_DIR: /source-archives/bodacc/current
      INPI_LOCAL_DATA_DIR: /source-archives/inpi
      DATA_LAKE_DIR: /data-lake
    ports:
      - "8000:8000"
    volumes:
      # Live code reload (dev) — local app/ directory
      - ./app:/app/app
      # Local operational scripts; final image also copies them.
      - ./scripts:/app/scripts
      # Host bind mounts under D:/PFE_volumes:
      # - ml-artifacts stores persisted model files
      # - source-archives stores official downloaded provider source files
      # - data-lake stores durable raw/clean/features parquet datasets
      - D:/PFE_volumes/ml-artifacts:/app/app/ml/artifacts
      - D:/PFE_volumes/source-archives:/source-archives
      - D:/PFE_volumes/data-lake:/data-lake
    depends_on:
      mongo:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - pfein-net

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pfein-worker
    command: ["python", "-m", "app.worker"]
    env_file:
      - .env
    environment:
      MONGO_URI: mongodb://mongo:27017
      MONGO_DB: ${MONGO_DB:-pfein}
      INGESTION_DATA_DIR: /source-archives
      BODACC_INIT_DATA_DIR: /source-archives/bodacc
      BODACC_CURRENT_DATA_DIR: /source-archives/bodacc/current
      INPI_LOCAL_DATA_DIR: /source-archives/inpi
      DATA_LAKE_DIR: /data-lake
    volumes:
      # Live code reload (dev) — local app/ directory
      - ./app:/app/app
      # Local operational scripts; final image also copies them.
      - ./scripts:/app/scripts
      # Host bind mounts under D:/PFE_volumes:
      - D:/PFE_volumes/ml-artifacts:/app/app/ml/artifacts
      - D:/PFE_volumes/source-archives:/source-archives
      - D:/PFE_volumes/data-lake:/data-lake
    depends_on:
      mongo:
        condition: service_healthy
    healthcheck:
      disable: true
    restart: unless-stopped
    networks:
      - pfein-net

  mongo:
    image: mongo:7
    container_name: pfein-mongo
    command: ["mongod", "--bind_ip_all", "--setParameter", "diagnosticDataCollectionEnabled=false"]
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_DB:-pfein}
    ports:
      - "27017:27017"
    volumes:
      # Persist MongoDB data on the Windows host.
      - D:/PFE_volumes/mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
    networks:
      - pfein-net

  mongo-express:
    image: mongo-express:1.0
    container_name: pfein-mongo-express
    profiles: ["dev"]
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_MONGODB_PORT: "27017"
      ME_CONFIG_BASICAUTH: "false"
    ports:
      - "8081:8081"
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - pfein-net

networks:
  pfein-net:
    driver: bridge
